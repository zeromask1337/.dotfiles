# Comments and blank lines are allowed.

RCS
.+,v

CVS
\.\#.+       # CVS conflict files / emacs lock files
\.cvsignore

\.svn
_darcs
\.hg

\.git
\.gitignore
\.gitmodules

.+~          # emacs backup files
\#.*\#       # emacs autosave files

^/README.*
^/LICENSE.*
^/COPYING

\.DS_Store

\.ssh/id_.*
\.ssh/known_hosts
\.gnupg/private-keys-v1.d
\.gnupg/secring.gpg

^/.dotfiles-private\.env
/config/gh/
/config/ngrok/ngrok.yml
# mise bootstrap config drives bootstrap itself — DON'T let stow own it, or a
# clone + `stow .` could symlink it to the repo's committed version and revert
# any freshly-scp'd/pre-bootstrap config. Keep it a real file the bootstrap runs from.
/config/mise/
^/lefthook\.yml
^/.lefthook/
